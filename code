<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>怪物抗性计算器</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: #f4f4f4;
            padding: 20px;
        }
        .container {
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            display: inline-block;
        }
        input {
            margin: 5px;
            padding: 8px;
            width: 100px;
        }
        button {
            margin-top: 10px;
            padding: 10px;
            background-color: #007bff;
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 5px;
        }
        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>怪物抗性计算器</h2>
        <label>技能伤害：</label>
        <input type="number" id="skillDamage" value="100"><br>
        <label>伤害加成：</label>
        <input type="number" id="damageBonus" value="1"><br>
        <label>劫掠者加成：</label>
        <input type="number" id="raiderBonus" value="1"><br>
        <label>怪物抗性：</label>
        <input type="number" id="resistance" value="50"><br>
        <button onclick="calculateDamage()">计算真实伤害</button>
        <h3>真实伤害：<span id="trueDamage">0</span></h3>
    </div>

    <script>
        function calculateDamage() {
            let skillDamage = parseFloat(document.getElementById("skillDamage").value);
            let damageBonus = parseFloat(document.getElementById("damageBonus").value);
            let raiderBonus = parseFloat(document.getElementById("raiderBonus").value);
            let resistance = parseFloat(document.getElementById("resistance").value);
            
            let trueDamage = skillDamage * damageBonus * raiderBonus * (100 / (100 + resistance));
            document.getElementById("trueDamage").textContent = trueDamage.toFixed(2);
        }
    </script>
</body>
</html>
